// ============================================================
// CONFIGURAÇÃO PÚBLICA DO SITE
// ============================================================
window.VIRAL_CONFIG = {
  
  // 💛 DOAÇÃO VIA PIX (QR Code estático gerado no app)
  // Preencha com a SUA chave Pix (aleatória recomendada), o nome do
  // recebedor (como no banco, até 25 caracteres) e a cidade (até 15).
  // Deixe qualquer um vazio para ocultar os botões de apoio.
  pixChave: '643f2583-df9e-4293-9583-8f0e3b214fe1',
  pixNome: 'ViralScript Ai',
  pixCidade: 'Santa Catarina',
  
  // 📊 Google Analytics 4 — cole aqui seu ID de medição
  // (formato "G-XXXXXXXXXX") para ativar o GA4.
  // Deixe '' para manter o analytics DESATIVADO (padrão).
  gaMeasurementId: '',
  
  // 📢 Google AdSense — v8.7 (MONETIZAÇÃO ATIVA)
  // Cliente da conta (verificado — presente no <head> de todas as páginas).
  // Os SLOTS (IDs dos blocos) você cria no painel APÓS a aprovação do
  // site e preenche abaixo: adsenseSlotTopo + adsenseSlotResultado.
  // Enquanto estiverem '', os espaços do gerador ficam ocultos.
  adsenseCliente: 'ca-pub-2359494647300339',
  adsenseSlotTopo: '',
  adsenseSlotResultado: ''
};